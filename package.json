{
  "name": "adityalf-readme",
  "version": "1.0.0",
  "description": "Terminal-style SVG generator for GitHub profile READMEs with Discord Rich Presence, VS Code activity log, and GitHub statistics.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "npm run build && node dist/server.js",
    "generate": "npm run build && npm run start"
  },
  "keywords": [
    "github-profile",
    "github-readme",
    "terminal",
    "lanyard",
    "svg-generator"
  ],
  "author": "AdityaLF",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.20.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0"
  }
}
